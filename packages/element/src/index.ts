/**
 * @cuestack/element — web components adapter.
 *
 * Empty in Wave 0. Wave 5 uses this to prove the kernel is genuinely
 * framework-agnostic rather than React-shaped.
 */
export const ELEMENT_WAVE = 0 as const
