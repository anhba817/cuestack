# Changesets

Version and changelog management for the four published Cuestack packages.
Run `pnpm changeset` to record a change; CI consumes these on release.

The four packages are **fixed**: they version together. A framework whose
adapters drift out of step with its core is a support burden, and until the API
settles a shared version number is cheaper than a compatibility matrix.
